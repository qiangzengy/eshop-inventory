<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiangzengy.eshop.mapper.ProductInventoryMapper">

	<update id="updateProductInventory" parameterType="com.qiangzengy.eshop.entity.ProductInventory">
           UPDATE product_inventory SET inventory_cnt = #{inventoryCnt} WHERE product_id = #{productId}
    </update>

    <select id="findProductInventory" resultType="com.qiangzengy.eshop.entity.ProductInventory">
          select product_id as "productId",inventory_cnt as "inventoryCnt" from product_inventory where product_id=#{productId}
    </select>
</mapper>
